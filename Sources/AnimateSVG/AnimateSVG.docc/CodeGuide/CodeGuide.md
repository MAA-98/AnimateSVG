# Code Guide

Guide to the codebase.

For ease of explanation, animation using a skeleton is explained. The case without a skeleton should be an obvious special case where the skeleton is `nil`. The guide will present the framework in a decreasing level of abstraction.

## API

### SVGLayer

The main part of the API is the `SVGLayer` struct. 
